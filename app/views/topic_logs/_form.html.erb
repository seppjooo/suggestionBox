
<center><%= form_for(@topic_log) do |f| %>
  <% if @topic_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_log.errors.count, "error") %> prohibited this topic_log from being saved:</h2>

      <ul>
      <% @topic_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.text_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.text_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :posted %><br>
    <%= f.date_select :posted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  </center>
<% end %>
